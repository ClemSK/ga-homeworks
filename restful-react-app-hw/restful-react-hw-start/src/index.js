import React from 'react'
import ReactDOM from 'react-dom'
import './styles/main.scss'
import App from './App'

// Here we render the page, this is a higher level than the App.js page

ReactDOM.render(<App />, document.getElementById('root'))
